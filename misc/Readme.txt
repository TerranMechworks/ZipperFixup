Installation
============

1. Copy `InstallFixup.bat` and `zipfixup.dll` to your game directory.
2. Run `InstallFixup.bat`. This will copy the game executable to `<Game name>fixup.exe` and patch it.
3. Run the new executable.
